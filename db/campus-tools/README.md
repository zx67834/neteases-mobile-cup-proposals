-- Campus tools schema (also ensured at runtime by ensureCampusToolsSchema)
-- Applied automatically when Next connects via DATABASE_URL.

-- See lib/persistence/campus-tools-schema.ts for the canonical statements.
